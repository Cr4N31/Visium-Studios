import team from "../../data/team";

function Studio() {
  {
    /*
      return (
    <section className="overflow-hidden">
      <div>
        <h1>The Studio</h1>
        <p>A multidisciplinary studio for brands with somewhere to go</p>
      </div>
      <div className="">
        <ul className="flex gap-8 absolute items-center justify-center">
          {team.map((t) => (
            <li>
              <img className="w-48 h-48" src={t.image} />
              <p className="text-sm">{t.name}</p>
              <p className="text-xs">{t.role}</p>
            </li>
          ))}
        </ul>
      </div>
    </section>
  );
}
     */
  }
}

export default Studio;
